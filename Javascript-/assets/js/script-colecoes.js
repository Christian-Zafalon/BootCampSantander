//Map
/*
const myMap = new Map( )

myMap.set('apple', 'fruit');

myMap.get(apple);

myMap.delete("apple")

myMap.get("apple")
*/

//Sets
/*
const mySet = new Set();

mySet.add(1);
mySet.add(4);
mySet.add(2);

console.log(mySet.has(6));
*/
/*
function getAdmin(map) {
    let admins = [];
    for ([key, value] of map) {
        if (value === 'Admin') {
            admins.push(key, value);
        }
    }
    return admins;
}

const usuarios = new Map();

usuarios.set('Christian', 'Admin');
usuarios.set('Matheus', 'Admin');
usuarios.set('Igor', 'Usuario');
usuarios.set('Marcos', 'Usuario');


function racas(mapeamento) {
    let array = [];
    for ([key, value] of mapeamento) {
        if (value === 'Cachorro') {
            array.push(key, value);
        }
    }
    return array;
}

const raca = new Map();

raca.set('Christian', 'Humano');
raca.set('Paula', 'Humano');
raca.set('Nick', 'Cachorro');
raca.set('Max', 'Cachorro');

console.log(racas(raca));
*/

/*
const meuArray = [30, 30, 40, 5, 223, 2049, 3034, 5];
function arrayFunction(arr) {
    const setar = new Set(arr);
    return [...setar];
}

console.log(arrayFunction(meuArray));
*/


